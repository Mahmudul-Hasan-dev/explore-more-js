function add(num1, num2) {
    console.log(num1 + num2);
    //arguments show all the parameter with index that have been sent when calling function
    //arguments is only available inside the function
    // arguments is array like object
    console.log(arguments)
}
add(10, 12);